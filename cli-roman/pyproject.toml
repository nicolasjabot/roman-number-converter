[project]
name = "cli-roman"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pytest>=8.4.1",
    "typer>=0.16.0",
]
[project.optional-dependencies]
test = ["pytest"]

[project.scripts]
roman = "roman_cli:main"


